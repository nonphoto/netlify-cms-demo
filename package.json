{
  "name": "netlify-cms-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "http-server",
    "build": "mkdir -p build && ./build-html.js content/blog/* > build/index.html && cp source/* build",
    "now-build": "npm run build"
  },
  "author": "Jonas Luebbers",
  "license": "MIT",
  "dependencies": {
    "markdown": "^0.5.0",
    "yaml": "^1.2.0"
  }
}
